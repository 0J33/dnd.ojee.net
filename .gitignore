node_modules/

# Builds — deployed from a local build, not committed, same as the sibling sites.
client/dist/
client/build/

# Secrets. MONGODB_URI is a live connection string; it never goes in the repo.
.env
.env.*
!.env.example

*.log
.DS_Store

# Deploy credentials.
deploy.local.env

# Impeccable scratch: review captures and question-page state
.impeccable/review/
.impeccable/questions/
