# Copy to .env (development) or .env.production.
# Where the API lives. Vite inlines this at build time, so it must be set
# before `npm run build`, not at runtime.
VITE_SERVER_URL=https://dnd-api.ojee.net
