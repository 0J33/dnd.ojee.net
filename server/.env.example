# Copy to .env (development) or .env.production and fill in.
PORT=5005
# Full mongodb:// or mongodb+srv:// connection string for the dnd database.
MONGODB_URI=
# Origin the browser is served from — CORS allow-list and cookie origin.
CLIENT_URL=https://dnd.ojee.net
